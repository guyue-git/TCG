"""服务 A —— 消息处理中枢.

职责（架构裁决 2026-09-03）：
* 本地 HTTP 服务监听 wechat_monitor.py 推送的消息文本；
* 按到达顺序持久化到 inbox JSONL（崩溃可重放）；
* 分类消息 → 调用注册的处理器（handlers/），线程池并行执行；
* 处理器为独立文件，新需求 = 新增一个处理器文件 + 注册一行。

与采集（monitor）彻底解耦：monitor 只负责「看到什么发什么」，
服务 A 只负责「收到什么干什么」。
"""

__version__ = "0.1.0"
